apiVersion: config.istio.io/v1alpha2
kind: RouteRule
metadata:
  name: api-split
spec:
  destination:
    name: smackapi
  route:
  - labels:
      version: v1
    weight: 50
  - labels:
      version: v2
    weight: 50